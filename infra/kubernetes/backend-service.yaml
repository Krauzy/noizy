apiVersion: v1
kind: Service
metadata:
  name: noizy-backend
  namespace: noizy
spec:
  selector:
    app: noizy-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
